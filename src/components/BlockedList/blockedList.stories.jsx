import React from "react";
import BlockedList from ".";

import "story.css";

const BlockedListInfo = {
  title: "页面组件/BlockedList",
  component: BlockedList,
};

export const Default = () => <BlockedList />;

export default BlockedListInfo;
