import React from "react";
import FileList from ".";

import "story.css";

const FileListInfo = {
  title: "页面组件/FileList",
  component: FileList,
};

export const Default = () => <FileList />;

export default FileListInfo;
