import React from "react";
import NoteList from ".";

import "story.css";

const NoteListInfo = {
  title: "页面组件/NoteList",
  component: NoteList,
};

export const Default = () => <NoteList />;

export default NoteListInfo;
