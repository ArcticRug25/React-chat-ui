import React from "react";
import Footer from ".";

import "story.css";

const FooterInfo = {
  title: "页面组件/Footer",
  component: Footer,
};

export const Default = () => (
  <div style={{ marginTop: 80 }}>
    <Footer />
  </div>
);

export default FooterInfo;
